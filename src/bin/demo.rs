/*use crate::vad_session::VadSession;
use crate::vad_environment::VadEnvironment;

fn main() {

    let audio_paths: Vec<String> = Vec::new();

    let vad_environment = VadEnvironment::new(("files/silero_vad.onnx").to_string());
    vad_environment.new
}
*/